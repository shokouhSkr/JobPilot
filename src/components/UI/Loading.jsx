import { BiLoader } from "react-icons/bi";

const Loading = () => {
  return (
    <div className="text-center text-primaryTxt">
      <BiLoader className="animate-spin text-4xl" />
    </div>
  );
};

export default Loading;
